Bootstrap: docker
From: mongo:5.0.2

%arguments
    DB_PORT=6534
    DB_PATH=~/minio/data

%environment
    export MONGO_INITDB_ROOT_USERNAME=fedn_admin
    export MONGO_INITDB_ROOT_PASSWORD=password

%startscript
    mongod --dbpath {{DB_PATH}} --port {{DB_PORT}}
