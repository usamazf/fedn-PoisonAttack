entry_points:
  train:
    command: python entrypoint.py train $ENTRYPOINT_OPTS --data_path="/var/data" --config_file="config.yaml"
  validate:
    command: python entrypoint.py validate $ENTRYPOINT_OPTS --data_path="/var/data" --config_file="config.yaml"
  # predict:
  #   command: python entrypoint.py predict $ENTRYPOINT_OPTS --config_file config.yaml